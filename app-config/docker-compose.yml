version: "3.8"
services:
  cara-app:
    image: cara-voila-app
    #        ports:
    #    - "8080:8081"
    links:
      - cara-router
  cara-router:
    image: cara-nginx-app
    ports:
        - "8080:8080"

